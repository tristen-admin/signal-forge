# Pending Mechanics

Cards whose ability/Called display **text** describes behavior the game engine doesn't actually implement yet. Nothing here is broken — the printed power/base effects still work — but the extra clause in the text is currently flavor only. Check this list before setting up a playtesting session; each entry needs real code before the described behavior is true in a match.

Standing practice: add an entry here any time a CALLED/CARD_RULES/ability text edit describes something the current schema/engine has no field or logic for, instead of only mentioning it in chat.

**Player-facing counterpart, added 7/27/26:** `FLAGGED_ABILITIES` (in the game file, near `COST_OVERRIDE`/`SHIELD_OVERRIDE`) is the in-game indicator version of this list — a whole-card outline ring on the battlefield card plus an explicit "not fully working yet" section in the detail view. Keep the two in sync: when a new Open entry below describes text that's genuinely still live on a player-visible card (not a dev-only gap like Tange Sazen's, where the overpromising text was already removed), add it to `FLAGGED_ABILITIES` too with a short plain-language reason. When an entry moves here to Resolved, remove it from `FLAGGED_ABILITIES` in the same pass.

**7/27/26 note:** this file had drifted badly from the real code — several "open" entries below had already been fixed in later passes (via a hardcoded name-check special case, or by simplifying the text to match a simpler real mechanic) without this file being updated to match. Before touching anything in a fresh "fix the backlog" pass, re-verify each entry against the actual code (`sf_query.py card "Name"` + a targeted grep for the card's name in `resolve()`/`applyCalled()`) rather than trusting this list at face value. See [[project_signalforge_sf_query_tool]] for the query tool and [[project_signalforge_pending_mechanics_batch]] for the full audit and batch that produced this rewrite.

## Open

### Moro — Called's "or banish from deck for +4" alternative isn't built
**Text says:** "return 1 banished card to hand, or banish 1 card from the top of your deck to give +4 to your conjured unit."
**What actually fires today:** only the first half — `recurPlain:1` (return 1 banished card to hand). The "or banish from the top of your deck for +4" branch has no field or logic yet; this is a real choice (pick one of two effects), same class of gap as Kravyn's below, not a simple declarative add.
**Why paused:** needs an actual choice prompt at Called-resolution time (return-from-banish vs. banish-from-deck-for-power), not just a new boolean flag — worth building alongside Kravyn's below rather than as a rushed one-off, since both need the same kind of "pick one of two Called effects" UI.

### Kravyn the Collector — on-kill hand-return is automatic, not a real choice (needs a decision)
**Text says:** "When this unit kills its enemy unit, you may choose to return this unit to the hand instead of placing it in the winners circle."
**What actually fires today:** `resolve()`'s win branch (name-hardcoded, alongside Skullchain Reaver) ALWAYS returns Kravyn to hand on any win, and a separate close-loss branch ALSO always returns him to hand on any loss by ≤3 power margin. Both fire unconditionally — there is no prompt, no "you may choose."
**Why this is paused, not built or silently rewritten:** two real options exist and they're a genuine gameplay-balance call, not just an implementation detail — (a) build a real kill-time choice modal (matches the printed text exactly, but the current always-return behavior on a close loss is itself a curious "consolation" mechanic worth keeping deliberately, or losing deliberately), or (b) simplify the printed text to match the current deterministic behavior (matches this session's precedent for Signal Diviner/Conduit of Chaos, where a text/code mismatch was resolved by rewriting the text instead of building new mechanics). Neither was picked without checking first.

### Tange Sazen — DM Activated ability, opponent-deck disruption has no real target
**Text says (second half):** "...and make your opponent return 1 card to the bottom of the deck."
**What actually exists:** no `DM_ACTIVATED_ABILITY['Tange Sazen']` entry at all today — only his passive `DM_EXCLUSIVE` (+8 power if your Winners Circle is empty), with a code comment directly above it already flagging this gap explicitly. `oppDrawPileBot` (the variable that would need to model this) is declared and reset every match but never populated or read anywhere — dead scaffolding from an unfinished feature, not a small fix.
**Why this is paused:** the bot's card pool isn't modeled as a real orderable deck anywhere in the engine (`OPP_CARDS`/`activeBot.pool` feed a padded random draw, not a persistent ordered array the way the player's `drawPile` is). Building this properly means modeling a real bot deck array first — a bot-AI architecture change, not a card-text fix — and deserves its own dedicated pass rather than a rushed addition alongside a text-mismatch batch.

## Resolved

### Pending-mechanics batch, shipped 7/27/26 — 8 built, 5 discovered already-fixed
Full audit + fixes: see [[project_signalforge_pending_mechanics_batch]] for the complete before/after and verification detail on each. Summary:

**Built for real this pass:**
- **Uso Oso** — Called: auto-selects the lowest-power hand card and raises it as a Death Remnant (new `autoRemnantLowest` CALLED field).
- **Legend Reborn, Keawe Kel'rua** — Conjure tutor now actually reduces the tutored card's Charge cost by 1 (reused the per-instance cost-discount field, renamed from `_dmScryDiscount` to the more general `_costDiscount` since it's no longer scry-exclusive).
- **Arch-Grim Korrin** — on-commit: looks at the top 2 cards of the deck, places 1 into an open support slot and raises the other as a Death Remnant. Replaced a stale, unrelated leftover hook (drew cards equal to remnant count) that matched an OLDER version of his ability text from before it was rewritten (see task #154) and had never been updated to match the current printed text.
- **Anorith Keeling** — `dmZeroCostThisDuel` now also zeroes Energy Spell costs in `castSpell()`/`buildSpellTray()`, not just unit/support conjure costs.
- **Ghorruk "Gnarly" Judarr** — `recurBuff` gained a `recurDest:'deck'` variant (bottom of deck, not hand) so his own text is finally honored without changing The Regenerating Horror's correct to-hand behavior; also wired the already-working `scryOne()` primitive to a new shared `scry` CALLED field.
- **Corvus, Veronica** — both also picked up the new shared `scry` field (Veronica additionally got the `add:2` her text always promised but her CALLED entry never had).
- **Darwin (Called)** — "play the top card of your deck into your open support circle" is now real (new `conjureTopToSupport` field), mirroring the existing Uso Oso/Valcarion pattern for this class of effect. Player-only, matching that same existing precedent (neither of those two has a bot-side mirror either).
- **Darwin (DM ability, 2nd clause)** — "whenever your hand is 2 or less cards after your draw phase, draw 2 and banish 1" is now a real recurring check inside `drawCard()`, armed by activating the ability and persisting for the rest of the match (not just the activation's own duel, unlike the first clause) — also fixed the ability's `desc` text, which still said "conjure cost" after the Shield rename.
- **Keawe Kel'rua** — "Repeat this effect if you have 1 or fewer support units this battle" is now a real second CARD_RULES clause using a compound `"and"` condition against the existing `rear_count` context variable — no new tracking primitive needed, just extending what the rules engine already exposes.

**Discovered already-fixed in a later pass, doc just never caught up:**
- **Kotei** — the win-conditional Charge+hand-return Called clause is real, implemented as a hardcoded `resolve()` name check (mirrors Kravyn/Tange Sazen's pattern).
- **Ahdorah Khaan, Determined Soul** — Called snipe-support clause is real (`snipeSupport:true`), from task #260.
- **Tange Sazen (Called half only)** — "returns to hand instead of the deck" is real, a hardcoded win-branch check in `resolve()`, from task #266. (The separate DM-ability opponent-deck-disruption clause is a different, still-open gap — see above.)
- **Signal Diviner, Conduit of Chaos, Kleydson** — both cards' printed text was simplified to match their real, working flat-number mechanics rather than the originally-envisioned choice/scaling mechanics being built. Text and code agree now; no mismatch remains.
- **Hanse Waltz / Lagertha Waltz — race-conditional Called bonus** — `twinBuff` already achieves this via the existing `fieldLink`/Link-group system (only fires when the supported Fighter shares a Link group with the support, e.g. the "waltz" group) rather than a direct `raceOf()` check — a different technical path than originally proposed, but the same functional promise the text makes. Bixie Bee's printed text no longer mentions a race-conditional bonus at all (rewritten at some point after the original entry was logged), so that third card is moot.

### Grim Korrin bot — Death Remnant now accumulates bot-side, shipped 7/24/26, commit `6ada58d`
Built exactly per the "Missing, concretely" list from the 7/22/26 deferral (kept below for the historical record): `oppDeathRemnants`/`oppRemnantPowSum()`/`oppDmRemnantPowOverride` as full mirrors of the player-side globals; `applyBotDeckMasterCommitEffects(oc)` mirroring `applyDeckMasterCommitEffects(pc)` (tier-1 `raiseRemnant`/`deathLingerBuffOverride` only — tier-2 vanilla draw deliberately not mirrored, out of scope for Korrin's own kit); `resolve()`'s `oppPow` baseline now includes `oppRemnantPowSum()`; the win branch pushes to `oppDeathRemnants` when `oc.abil` contains "Death Remnant" (mirrors `isDeathRemnant` exactly, including the `REMNANT_POW`/override lookup and board-log message); the lose branch clears `oppDeathRemnants` (mirrors the existing "your Remnants disperse" clear); both `oppEvalPow()` and `resolve()`'s bot-side CARD_RULES ctx (`_octx`) read real `oppDeathRemnants.length` instead of a hardcoded 0; `resetMatchState()` clears both new globals.
**Verified live, not just syntax-checked:** `applyBotDeckMasterCommitEffects` with a mock `activeBot.dmCard='Arch-Grim Korrin'` correctly pushed `{name, pow:17}` (self_pow) and set the override to 10; `oppEvalPow('Arch-Grim Korrin')` returned 17 with zero remnants held and 25 with two held (matching his printed +4/Remnant CARD_RULES exactly — confirms a bot now correctly values conjuring him higher once Remnants are already active); `resetMatchState()` confirmed clearing both globals.
**Original deferral note, for context:** flagged mid-session 7/22/26 while working unsupervised, deliberately not rushed then because a wrong wire-up would silently distort bot difficulty across every match using this bot — deserved a dedicated pass with real verification rather than a solo overnight build alongside structurally-safer fixes. Picked back up 7/24/26 with exactly that dedicated pass.

### DM Activated Abilities — 11 cards built as real `run:function()` code, shipped 7/20/26
Built real, working `DM_ACTIVATED_ABILITY` entries (not passive `DM_EXCLUSIVE` — every one of these plain-language asks described a one-time action sequence, matching the *activated*, once-per-match pattern already established for Kravyn/Sister Mire/Black Wings, not the always-on declarative `DM_EXCLUSIVE` pattern). New shared engine primitives added to support them:
- **`scryOne(onDone)`** — a real interactive modal: look at the top card of the deck, choose Keep on Top or Move to Bottom. Used by Keawe Kel'rua, Arch-Grim Korrin, and Corvus's new abilities.
- **Turn-scoped effect flags** (`dmUnitsReturnToHandThisDuel`, `dmSupportAddPowThisDuel`, `dmSupportDoubleCostThisDuel`, `dmNullifyOppSupportsThisDuel`, `dmZeroCostThisDuel`) — set on activation, read inside `resolve()`'s support loops / `conjureCost()`, cleared at the next duel-start reset (same pattern as the pre-existing `dmSupportBonusThisDuel`/`handBanishedThisTurn`).
- **`dmNextConjureDouble`** and **`dmAhdorahBonusPending`** — single-shot deferred flags, consumed and cleared at the same point as the pre-existing `dmNextConjureBonus`.
- **`card._costDiscount`** (renamed 7/27/26 from `_dmScryDiscount` once Legend Reborn's tutor reused it for a non-scry source — see the batch above) — a per-card-instance cost discount, checked inside `conjureCost()`, set on a specific card object by reference (not a global override).

Card-by-card: **Kravyn the Collector** (replaced — shuffle banish into deck, next conjure +2, wins return to hand this duel), **Keawe Kel'rua** (new — draw 1, scry 1, conditional -1 cost on the scried card), **Kotei** (new — draw 2, bottom your highest card, supports add their own power this duel), **The Bronzed Beast, Hanse Waltz** (new — hand≤3 conditional draw 2 + Charge), ~~**Arch-Grim Korrin** (new, added alongside his existing passive)~~ **removed 7/23/26, see correction below**, **Ghorruk "Gnarly" Judarr** (new — raise a Remnant at hand+banish size), ~~**La "La" Ballora** (new, added alongside her existing passive)~~ **removed 7/23/26, see correction below**, **Conduit of Chaos, Kleydson** (replaced — permanently upgrades to the full 3-spell Chaos Trinity via the pre-existing `energyLoadoutOverride` mechanism, +2 Charge), **Sister Mire, Wailing Nightmare** (replaced — hand to deck bottom, Winners Circle back to hand, draw 3, next conjure +5), **Black Wings, Ossian Drell** (replaced — hand to banish, draw 5, next conjure scales by banish size), **Corvus** (new — banish top 2, draw 1, scry), **Darwin** (new, first clause only at the time — see the 7/27/26 batch above for the 2nd clause), **Ourevos, the Golden Dragon** (new — next conjure's power doubled), **Anorith Keeling** (new, hand/support costs only at the time — see the 7/27/26 batch above for spell costs), **Ahdorah Khaan** (new — nullify opponent's supports this duel, draw 2 if they called none), ~~**Tange Sazen** (new, draw-1 half only)~~ **removed 7/23/26, see correction below**.

### Correction, 7/23/26 — 4 cards had BOTH a passive and an activated Deck Master ability; only one should exist
Direct report: "he has 2 deckmaster abilities, one activate and one passive. It shouldn't be that powerful, needs to only be one. The passive skill needs to overwrite the activated ability." Checked every `DM_EXCLUSIVE`/`DM_ACTIVATED_ABILITY` pair via the real runtime objects (not text search — two of the four have names containing embedded quotes/apostrophes that broke a first regex-based pass, so `Object.keys()` was used instead to be certain). Found **4** cards double-dipping, not just the one reported: **Arch-Grim Korrin**, **Bannerlord Cassian**, **Tange Sazen**, and **La "La" Ballora, the Broodqueen**. Removed each one's `DM_ACTIVATED_ABILITY` entry entirely — the passive `DM_EXCLUSIVE` stays as their one real Deck Master bonus. Verified live for two of the four (Korrin, Ballora): no activate button renders in the Instant Window strip, and the passive still fires correctly on commit/resolve (Korrin raises a self-power Remnant; Ballora's `+1 pow/Winners-Circle-card` still applies). The other two (Cassian, Tange Sazen) share the identical code shape and were verified via a zero-overlap check on the real objects, not re-tested individually in a live duel.

### Squad 19 / Tosa Pact glue-common batch, shipped 7/23/26, commit `f833527`
10 new commons/rares to fill out Ahdor's Pride (was 5 cards) and Keawe's Circle (characters only, no cadet-corps depth): Squad 19 Vanguard/Medic/Quartermaster/Scout, Ahdorah Khaan (Squad 19 Recruit — Rare low-rarity sibling printing), Fenrik Vench (Rare, Keawe's Ch.1 rival), Yard Sparring Partner, and 3 shared [Tosa Pact] splash commons (Barracks Drill Sergeant, Cadet Corps Recruit, Writ-Sergeant's Ledger) added to both presets. Authorized directly ("just make it") rather than via the line-by-line balance-review gate, since this is additive roster-filling, not a rebalance of existing numbers — see [[project_signalforge_squad19_cadet_batch]] and `BALANCE_PROPOSALS.md` §2 for the full proposed-vs-shipped diff.

**Real bug caught during verification — CARD_RULES condition-shape gotcha, worth flagging generically:** `evalRuleCond(cond, ctx)` resolves a condition's right-hand side from exactly one of three keys — `n` (a number literal), `s` (a string literal), or `v2` (ANOTHER ctx field name to look up, not a literal). Wrote Ahdorah Khaan, Squad 19 Recruit's rule as `{"v":"last_result","op":"==","v2":"lose"}`, intending a literal string compare — but `v2` treats `"lose"` as a ctx field name, so it actually evaluated `ctx.last_result === ctx.lose` (`ctx.lose` is `undefined`), silently never firing. Correct form for a literal string compare is `"s":"lose"`. Caught by calling `applyCardRules()` directly with both a passing and failing ctx and diffing the returned power — the "should fire" case returned no change, which is what exposed it. No other CARD_RULES entry in this codebase currently makes this mistake (checked); flagging here so a future string-comparison rule doesn't repeat it. This also confirms `<=`/`>=` both exist as real operators (previously only guessed at) — full confirmed op set: `==`, `!=`, `>`, `<`, `>=`, `<=`.
